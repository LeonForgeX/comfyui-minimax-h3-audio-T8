# 正式 Topaz 高清后处理（可选）

先用 Environment 图填写正式程序、模型定义与模型数据目录；该图只检查签名和接口，不跑增强。
Video 图接原生视频文件与环境输出。默认2x需自行在正式 Topaz 准备对应权重；
Iris1x及2x短片已完成机械验证：2x为72帧2048×1024，原音频包和PCM保留。
人物2x/1.5x、游戏2x及既有24秒2x样片已获对应画音认可，不代表所有素材。4x未实测，星光按用户要求暂停，不算已完成。

`1x`是原尺寸增强；`2x/4x`是宽高倍率，不是插帧。默认`parameters_json={}`使用程序默认。
高级`size_mode=target_dimensions`按指定宽高输出并忽略倍率：先Topaz增强，再调整
增强结果到目标尺寸。须保持原比例、1至4倍、偶数32至8192。默认scale及旧工作流不变。
1.5x短片已通过1536×768/72帧与原音频检查，并获对应画音认可，不代表任意尺寸/模型均验收。
输出无损MOV/MKV母版：AAC源走PNG48-MOV以保留编码延迟，其他已支持音轨走FFV1-MKV；逐包及解码PCM均核对。可后接原生Save Video另存预览，但另存编码后的音轨需另验；不覆盖原片，不自动下载。
导入Video图后把示例`source.mkv`替换为自己的视频。不要使用裁切/未保存的帧批次输入。

完整安装、参数与限制见[说明](../../../docs/TOPAZ_EXP.md)。这些图通过实际Core API/序列化检查，
尚未完成浏览器往返。正式星光组件已安装，但实测授权检查失败；需在正式Topaz确认
登录及星光权限。不会退到`G:/star2.6`或改动授权文件。
